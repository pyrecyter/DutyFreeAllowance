import 'package:flutter/material.dart';

class OurApps extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Image.asset('assets/visible_tec.png'), //image
    );
  }
}